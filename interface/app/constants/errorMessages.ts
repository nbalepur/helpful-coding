import { ENV } from '../config/env';

/**
 * Error message constants used across the application
 */
export const ERROR_TRY_AGAIN = `The AI agent failed! Please try submitting your query again, or try hitting 'Clear Messages'. If the problem persists, please contact us at ${ENV.FROM_CONTACT_EMAIL}.`;
